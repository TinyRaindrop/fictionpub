# fictionpub
FB2 to EPUB3 ebook converter with CLI / GUI

Build package
```python -m pip install build
python -m build
pip install dist/fictionpub-*.whl
```

Editable mode
`pip install -e .`

Run GUI
`fictionpub`

Run CLI
`fictionpub [input files] [other args]`
